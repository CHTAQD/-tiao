vue 

v-for循环

v-for 循环可以循环数组，也可以循环对象，可以循环指定内容的数组，也可以循环默认内容的数据

v-model是实现双向绑定的但前提得现有数据

单选按钮的value是不会显示的

template  前端模板标签

该标签会把内部所有的html代码看作是一整个模板，如果给template标签加上v-if就会显示和隐藏整个模板，

es6 的方法函数新写法

es5：属性名：function（）{}

es6：属性名(){}

箭头函数

let和const关键字

for of 值遍历

Math。numberstring  object

新的api/接口(方法)

循环数组循环对象，循环默认值。

循环数组：在要循环的标签中写入v-for="数组项  in  数组" ，之后在主模块的data属性中定义要循环的数组，最后如果需要指定数据，再把数据写入就可以了

循环对象

1.v-for="对象的键值 in 指定对象"

2.v-for="(value,key) in 指定对象"

3.v-for ="(value,key,index) in 指定对象"

循环键值，键名 以及索引

v-for也可以用来循环指定的值（数组）

指定的数组是几，就会循环几次

v-for="当前索引加1 in 数字 "


h5 input date属性    number属性



















