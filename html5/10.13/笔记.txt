scale(x,y)

x：横坐标的方向缩进

y:纵坐标的方向缩进

注意：缩放方法并不仅能够缩放图形，还能改变图形位置，如果想要图形不动，就需要配合translate使用

刮刮乐的实现

首先以奖品图片为canvas背景图，然后在画布上绘制一个黑色的填充矩形

鼠标按下并移动的时候，以鼠标经过的点为圆心，画圆

当擦处到一半的时候，就要清楚整个黑色矩形

ctx.getImageData()

获取画布上的所有像素点的rgba的值，并把获取到的所有值，按顺序拼凑成一个数组


事件对象
获取鼠标相对于页面的位置的横纵坐标的方法是一个时件对象的方法e.pageX   e.pageY

jQuery中用来给元素绑定事件的是on方法而解除绑定的是off方法


ctx.globalCompositeOperation="destination-out"
显示下面的内容

当绘制两个图形，有相交的部分，只显示旧图形，与新图形不相交的部分


值	描述
source-over	默认。在目标图像上显示源图像
。
source-atop	在目标图像顶部显示源图像。源图像位于目标图像之外的部分是不可见的。

source-in	在目标图像中显示源图像。只有目标图像内的源图像部分会显示，目标图像是透明的
。
source-out	在目标图像之外显示源图像。只会显示目标图像之外源图像部分，目标图像是透明的。

destination-over	在源图像上方显示目标图像。
destination-atop	在源图像顶部显示目标图像。源图像之外的目标图像部分不会被显示。

destination-in	在源图像中显示目标图像。只有源图像内的目标图像部分会被显示，源图像是透明的。

destination-out	在源图像外显示目标图像。只有源图像外的目标图像部分会被显示，源图像是透明的。

lighter	显示源图像 + 目标图像。

copy	显示源图像。忽略目标图像。
cd
xor	使用异或操作对源图像与目标图像进行组合。


Math.sin和Math.cos

分别是用来获取正弦函数和余弦函数的Math对象的方法

Math.sin(2*Math.PI*n/360)

Math.cos(2*Math.PI*n/360)





















































































