window{
		属性{
			1、screenLeft\screenX
			   screenTop\screenY
			2、innerWidth\innerHeight
			document.documentElement.clientWidth
			document.documentElement.clientHeight
			3、parent、top、self、
				length
			4、status
		}
		方法{
			1、moveBy()
			   moveTo()
			2、resizeBy()
				resizeTo()
			3、scrollBy()
			   scrollTo()
			4、setInterval()
			   clearInterval()
			   setTimeout()
			   clearTimeout()
			5、close()
			   open('路径','名称','属性')
			   opener()
		}
	}
	history{
		属性{
			length
		}
		方法{
			back()
			forward()
			go()
			0 刷新  1前进   -1后退
		}
	}
	location{
		属性{
			href
			search
		}
		方法{
			assign()
			reload()
			replace()
		}
	}
	window对象{
		document对象
		history对象
		loaction对象
		screen对象
		navigator对象
	}
