			笔记

模板引擎：artTemplate(腾讯)；
Template.js<==>jquery.js
template-native.js<==>jquery.js
定义。template。js当成一个用来实现编写模板功能的插件，所以在需要使用编写模板功能的时候要先引用template.js

script的标签type 属性中text/后面跟的谁，就表示这个script标签中的文本内容按照谁的语法格式来解析

模板初始化的作用：

是用来在js代码里获取到我们之前定义的模板
初始化模板引擎的方法：template()

传参（两个）

-1.要确定的模板script标签的id

2.传入一个对象，对象的内容使要在模板中展示的数据

、




妧一峰

张鑫旭

sea.js

xmlHttpRequery

对象.setRequestHeader("Content-Type","application/x-www-form-urlencoded"
请求头   必填

0：请求未初始化（还没有调用 open()）。

     1：请求已经建立，但是还没有发送（还没有调用 send()）。

     2：请求已发送，正在处理中（通常现在可以从响应中获取内容头）。

     3：请求在处理中；通常响应中已有部分数据可用了，但是服务器还没有完成响应的生成。

     4：响应已完成；您可以获取并使用服务器的响应了。

